[tool.poetry]
name = "pynsure"
version = "0.1.0"
description = ""
authors = ["Justin Stitt <justinstitt@google.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
